# Market Caffeinate

This service is a shell-script wrapper, not a Go or Python application.

The runnable source is:

- `scripts/run_caffeinate.sh`
- `scripts/start_mac.sh`
- `scripts/stop_mac.sh`
- `scripts/market_caffeinate.plist`

There is no missing compiled-application source here; the script itself is the source.

