# Filters Service

This directory currently contains deployment artifacts only:

- launchd plist
- start/stop scripts
- compiled binary

The original implementation source for `filters` is not present in this repository snapshot and could not be recovered from the visible Git history or local Trash.

Operational status:
- deployment assets are preserved in Git
- implementation source is still unavailable

